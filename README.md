Assumed MySQL as the primary database and used bootstrap.
I was confused while running the server. Because the server was not running due to a dependency not being installed.
 Improve for Production, critical Must Have User login and signup system, Search and filtering, rich text editor instead of plain textarea
